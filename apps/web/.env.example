# CI Analyst Web Environment Selector
#
# Copy ONE of the following files to `apps/web/.env.local`:
# - `.env.mock.example`    for UI-only local mock mode
# - `.env.remote.example`  for routing to orchestrator (sandbox/prod)
#
# This file is intentionally a pointer to keep setup intuitive.
