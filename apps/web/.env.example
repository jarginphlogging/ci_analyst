# CI Analyst Web Environment Selector
#
# Real runtime file:
# - `.env.local` (already provided in repo; edit directly)
#
# Optional references:
# - `.env.web-mock.example`      for UI-only local mock mode
# - `.env.orchestrator.example`  for routing to orchestrator (sandbox/prod)
#
# This file is intentionally a pointer to keep setup intuitive.
