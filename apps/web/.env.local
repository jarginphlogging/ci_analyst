# CI Analyst Web Runtime Config
# Edit this file directly. No copy step required.

# web_mock | orchestrator
WEB_BACKEND_MODE=web_mock

# Used when WEB_BACKEND_MODE=orchestrator
ORCHESTRATOR_URL=http://localhost:8787

# Used when WEB_BACKEND_MODE=web_mock
WEB_MOCK_STATUS_DELAY_MS=650
WEB_MOCK_TOKEN_DELAY_MS=110
WEB_MOCK_RESPONSE_DELAY_MS=400
