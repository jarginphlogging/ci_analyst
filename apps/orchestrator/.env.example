# CI Analyst Orchestrator Environment Selector
#
# Copy ONE of the following files to `apps/orchestrator/.env`:
# - `.env.mock.example`     for static local mock mode
# - `.env.sandbox.example`  for Anthropic + local Cortex + SQLite
# - `.env.prod.example`     for Azure + Snowflake Cortex
#
# This file is intentionally a pointer to keep setup intuitive.
