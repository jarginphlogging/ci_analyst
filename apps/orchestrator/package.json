{
  "name": "@ci/orchestrator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8787 --reload",
    "start": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8787",
    "lint": "python3 -m py_compile app/*.py app/providers/*.py app/prompts/*.py app/services/*.py tests/*.py",
    "test": "python3 -m pytest -q",
    "build": "python3 -m compileall app"
  }
}
