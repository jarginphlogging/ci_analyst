# This repository uses app-level env files.
#
# Do not use a root `.env` for normal runtime.
# Use:
# - `apps/orchestrator/.env` (backend)
# - `apps/web/.env.local` (frontend)
#
# Optional reference templates:
# - `apps/orchestrator/.env.mock.example`
# - `apps/orchestrator/.env.sandbox.example`
# - `apps/orchestrator/.env.prod.example`
# - `apps/web/.env.web-mock.example`
# - `apps/web/.env.orchestrator.example`
